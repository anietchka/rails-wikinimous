<h1>List of articles</h1>

<ul>
  <% @articles.each do |article| %>
  <li>
    <div class="title">
      <%= link_to article.title, article_path(article)  %>
    </div>
    <div class="content">
      <%= article.content %>
    </div>
   </li>
  <% end %>
</ul>
